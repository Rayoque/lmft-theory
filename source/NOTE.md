Original .docx packets live here locally but are gitignored: the repo is
public to serve Pages, and publishing the complete original documents goes
further than the excerpts the app needs. extracted/ has the parsed text the
pipeline actually reads. Drop the five .docx back here to re-run extract.py.
